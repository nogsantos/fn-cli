const expect = require('chai').expect;

describe('function', function () {
    describe('All', function () {
        it('should be pass for now', function () {
            expect(true).to.be.true;
        });
    });
});
